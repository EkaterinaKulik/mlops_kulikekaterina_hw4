name: 'fraud_dbt_project'
version: '1.0.0'
config-version: 2

profile: 'fraud_dbt_clickhouse'

model-paths: ['models']
macro-paths: ['macros']
test-paths: ['tests']
seed-paths: ['seeds']
analysis-paths: ['analysis']

target-path: 'target'

clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  fraud_dbt_project:
    +materialized: view

    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts
